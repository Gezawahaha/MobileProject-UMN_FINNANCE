<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.ac.id.project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconumn"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Studentsup"></activity>
        <activity android:name=".Calender" />
        <activity android:name=".email" />
        <activity android:name=".Paymentmethod" />
        <activity android:name=".History" />
        <activity android:name=".Payment" /> <!-- <activity android:name=".Notification"></activity> -->
        <!--
<activity android:name=".Loading">

        </activity>
        <activity android:name=".LoginActivity"></activity>
        -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".profile" />
    </application>

</manifest>